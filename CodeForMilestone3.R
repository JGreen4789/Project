a<-read.csv("Demographic_Statistics_By_Zip_Code.csv")
library(e1071)
mean(a$COUNT.FEMALE)
mean(a$COUNT.MALE)
mean(a$COUNT.PARTICIPANTS)
mean(a$COUNT.WHITE.NON.HISPANIC)
sd(a$COUNT.MALE)
sd(a$COUNT.FEMALE)
sd(a$COUNT.PARTICIPANTS)
sd(a$COUNT.WHITE.NON.HISPANIC)
skewness(a$COUNT.PARTICIPANTS)
skewness(a$COUNT.FEMALE)
skewness(a$COUNT.MALE)
skewness(a$COUNT.WHITE.NON.HISPANIC)